# Materiales fuente de los cursos: son libros y artículos con derechos de
# autor, no código ni contenido del proyecto — no pertenecen a este repo.
/Materiales del curso *
/materiales-fuente/

# Archivos temporales de Word
~$*

# Nunca versionar credenciales sueltas en la raíz del proyecto
ANTHROPIC_API_KEY*
*API_KEY*.docx
